
    <%- include("../../shared/includes/head",{pageTitle:"Signup"}) %>
    <link rel="stylesheet" href="/styles/forms.css">
    <link rel="stylesheet" href="/styles/auth.css">
  </head>
  <body>
    <%- include("../../shared/includes/header") %>
    <main>
      <form action="/signup" method="post">
        <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
        <h1>Create Account</h1>
        <% if(userData.errorMessage){ %>
          <span class="alert"><%=userData.errorMessage %></span>
          <% }else{ %>
        <span>Register now for exclusive offers, order updates and faster checkout</span>
        <% } %>
        <section>
          <p>
          <label for="email">Email Address</label>
          <input type="email" id="email" name="email" value="<%=userData.email %>" required />
        </p>
        <p>
          <label for="confirm-email">Confirm Email Address</label>
          <input
            type="email"
            id="confirm-email"
            name="confirm-email"
            value="<%=userData.confirmEmail %>"
            required
          />
        </p>
        <p>
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            value="<%=userData.password %>"            
            required
          />
        </p>
        <hr />
        <p>
          <label for="fullname">Full Name</label>
          <input type="text" id="fullname" name="fullname" value="<%=userData.fullname %>" required />
        </p>
        <p>
          <label for="street">Street</label>
          <input type="text" id="street" name="street" value="<%=userData.street %>" required />
        </p>
        <p>
          <label for="postal">Postal Code</label>
          <input
            type="text"
            id="postal"
            name="postal"
            minlength="5"
            maxlength="5"
            value="<%=userData.postal %>"
            required
          />
        </p>
        <p>
          <label for="city">City</label>
          <input type="text" id="city" name="city" value="<%=userData.city %>" required />
        </p></section>
        <button class="btn">Create Account</button>
        <p id="switch-form">
          Already have an account? <a href="/login">Sign in</a>
        </p>
      </form>
    </main>
    <%- include("../../shared/includes/footer") %>
